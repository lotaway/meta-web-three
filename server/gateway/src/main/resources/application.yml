spring:
  profiles:
    active: dev
server:
  port: 10081
---
spring:
  application:
    name: gateway
    gateway:
      discovery:
        locator:
          enabled: true # 自动基于注册信息转发
          lower-case-service-id: true