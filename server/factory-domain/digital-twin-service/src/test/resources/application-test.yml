spring:
  application:
    name: digital-twin-service-test
  profiles:
    active: test
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 0

digital-twin:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173

logging:
  level:
    com.metawebthree: DEBUG
    org.springframework.kafka: DEBUG