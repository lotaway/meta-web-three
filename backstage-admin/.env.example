# ==================== Backend API ====================
VITE_BASE_SERVER_URL=http://localhost:10081 # backend api base url (via Gateway)
VITE_UPLOAD_URL=/media-service/media # file upload endpoint (backend supports S3/MinIO/local)

# Backend storage type configured server-side via storage.type
# Frontend always uploads through the backend /media endpoint
