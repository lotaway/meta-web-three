{
  "name": "meta-web-three",
  "version": "1.0.2-20230225",
  "description": "",
  "author": "",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "node run-dev.js",
    "build": "docker build -t wayluk/meta-web-three:1.0.2 .",
    "init:docker:env": "docker compose -f ./docker-compose.dataenv.yaml up",
    "stop:docker:env": "docker compose -f ./docker-compose.dataenv.yaml stop",
    "delete:docker:env": "docker compose -f ./docker-compose.dataenv.yaml down",
    "delete:system:docker:env": "docker stop $(docker ps -aq) && docker rm $(docker ps -aq)"
  },
  "dependencies": {
    "caniuse-lite": "^1.0.30001519",
    "ethers": "^6.13.4"
  }
}
