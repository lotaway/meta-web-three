/**
 * 开发环境下的配置
 */
module.exports = {
    //post: 3000,   //  设置端口号，次级优先权，优先级最高的是process，最低是www里默认的3000
    cookieSecret: "what do you want to do?",
    mongodb: 'mongodb://127.0.0.1:27017/test'   //  mongodb数据库
};